                  pointers


#include<stdio.h>
int main()
{
    /*int a;
    scanf("%d",&a);
    int *ptr;
    ptr =&a;
    printf("a is %d\n",a);
    printf("pointer value %d\n",*ptr);
    printf("memory value of ptr %u\n",ptr);
    printf("memory a %u\n",&a);
    printf("memory ptr %u\n",&ptr);*/

    /*char ch='A';
    char *ptr;
    ptr= &ch;
    printf("%c\n",*ptr);
    printf("%d\n",ptr);
    printf("%d\n",ptr+2);
    printf("sizeof %d\n",sizeof(ptr));
    printf("sizeof %d\n",sizeof(ptr+2));*/

    /*int a=10;
    int *ptr,**ptr1;
    ptr =&a;
    ptr1 =&ptr;
    printf("the a value %d\n",a);
    printf("address %u\n",ptr);
    printf("value of ptr %d\n",*ptr);
    printf("adress of ptr %u\n",&ptr);
    printf("value of ptr1 %d\n",**ptr1);
    printf("memory of ptr1 %u\n",&ptr1);*/
 
 
 
}



                                     ARRAY


#include<stdio.h>
int main()
{
int size,even=0,odd=0;
    scanf("%d", &size);
    int arr[size];
    for(int itr=0;itr<size;itr++)
        scanf("%d",&arr[itr]);
    for(int itr=0;itr<size;itr++)
    {
        if(arr[itr]%2==0)
           even++;
         else
            odd++;
    }
    printf("even count= %d\n",even);
    printf("odd count= %d\n",odd);
}




left rotation


#include<stdio.h>
int main()
{
int size,itr;
    scanf("%d", &size);
    int arr[size];
    for(int itr=0;itr<size;itr++)
        scanf("%d ",&arr[itr]);
    int temp;
    int count;
    scanf("%d",&count);
    for(int c=1;c<=count;c++)
    {
      temp=arr[0];
    for(itr=0;itr<size-1;itr+=1)
        arr[itr]=arr[itr+1];
      arr[size-1]=temp;

    }
   for(itr=0;itr<size;itr++)
    printf("%d",arr[itr]);
 return 0;
}
 



ARRAY


#include<stdio.h>
void main()
{
    int size,itr, sum=0;
    scanf("%d",&size);
    int arr[size];
    for(itr=0;itr<size;itr++)
        scanf("%d",&arr[itr]);
    for(itr=0;itr<size;itr+=1)
    {
        arr[itr]=sum+arr[itr];
         sum=arr[itr];
    }
    for(itr=0;itr<size;itr+=1)
        printf("%d ",arr[itr]);

}
 OUTPUT
5
10 20 30 40 50

10 30 60 100 150


max min



int size,itr;
    scanf("%d",&size);
    int arr[size];
    for(itr=0;itr<size;itr++)
        scanf("%d",&arr[itr]);
    int max=arr[0],min=arr[0];
     for(itr=1;itr<size;itr++)
    {
        if(arr[itr] > max)
            max = arr[itr];
        if(arr[itr]<min)
            min=arr[itr];
    }
    printf("maximum is %d\n", max);
    printf("minimum is %d\n",min);

OUTPUT
5
10 20 3 40 50
maximum is 50
minimum is 3



sum of 2d array

int main()
{
    int row,col,sum=0;
    scanf("%d%d",&row,&col);
    int arr[row][col],i,j;
    for(i=0;i<row;i+=1)
    {
        for(j=0;j<col;j+=1)
            scanf("%d",&arr[i][j]);
    }
    for(i=0;i<row;i+=1)
    {
        for(j=0;j<col;j+=1)
            sum+=arr[i][j];

    }
    printf("%d",sum);
    return 0;
}


output

3 3
1 2 3 4 5 6 7 8 9

sum is 45


sum of each row

int row,col;
    scanf("%d%d",&row,&col);
    int arr[row][col],i,j;
    for(i=0;i<row;i+=1)
    {
        for(j=0;j<col;j+=1)
            scanf("%d",&arr[i][j]);
    }
    int sum;
    for(i=0;i<row;i+=1)
    {
        sum=0;
        for(j=0;j<col;j+=1)
            sum+=arr[i][j];
            printf("%d",sum);
    }


    return 0;


max of each row in array

int main()
{
   int row,col,max;
   scanf("%d%d",&row,&col);
   int arr[row][col],i,j;
   //read
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
        scanf("%d",&arr[i][j]);
   }
   //write

   for(i=0;i<row;i+=1)
   {
       max=arr[0][0];
       for(j=0;j<col;j+=1)
        if(arr[i][j]>max)
        {
            max=arr[i][j];
        }

        printf("%d ",max);
   }
  return 0;
}


max element in total array

int main()
{
   int row,col,max;
   scanf("%d%d",&row,&col);
   int arr[row][col],i,j;
   //read
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
        scanf("%d",&arr[i][j]);
   }
   //write
 max=arr[0][0];
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
        if(arr[i][j]>max)
        {
            max=arr[i][j];
        }


   }
    printf("%d ",max);
  return 0;
}

OUTPUT
3 3
35 46 57 68 89 53 26 56 34
89


SUM OF TWO MATRIX

int main()
{
   int row,col;
   scanf("%d%d",&row,&col);
   int arr1[row][col],arr2[row][col],i,j;
   //read
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
        scanf("%d",&arr1[i][j]);
   }
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
        scanf("%d",&arr2[i][j]);
   }
   int arr3[row][col];
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
            arr3[i][j]=arr1[i][j]+arr2[i][j];
   }
   for(i=0;i<row;printf("\n"),i+=1)
   {
       for(j=0;j<col;j+=1)
          printf("%d ",arr3[i][j]);
   }



  return 0;
}

OUTPUT
2 2 // MATRIX SIZE
1 2
3 4   // FIRST MATRIX
1 2
3 4  // SEC MATRIX
2 4
6 8  // TOTAL SUM


comparison of two matrix equal or not

int main()
{
   int row,col;
   scanf("%d%d",&row,&col);
   int arr1[row][col],arr2[row][col],i,j;
   //read
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
        scanf("%d",&arr1[i][j]);
   }
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
        scanf("%d",&arr2[i][j]);
   }
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
       {
         if(arr1[i][j]!=arr2[i][j])
            break; //this only break the inner loop because loop is given inside a inner loop
       }
       if(j<col)
        break;
   }
   if(i<row)
    printf("not equal");
   else
    printf("equal");





  return 0;
}

OUTPUT
2 2

1 2
3 4

1 2
4 5

not equal

identity matrix or not


int main()
{
   int row,col;
   scanf("%d%d",&row,&col);
   int arr[row][col],i,j;
   //read
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
        scanf("%d",&arr[i][j]);
   }

   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
       {
           if(i==j)
           {
               if(arr[i][j]!=1)
                break;
           }

           else
       {
           if(arr[i][j]!=0)
            break;
       }
       }

   if(j<col)
    break;
}
if(i<row)
printf("not identity");
else
printf("identity");

  return 0;
}

OUTPUT
2 2

1 0
0 1
identity


TOEPLITZ MATRIX

int main()
{
   int row,col;
   scanf("%d%d",&row,&col);
   int arr[row][col],i,j;
   //read
   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
        scanf("%d",&arr[i][j]);
   }

   for(i=0;i<row;i+=1)
   {
       for(j=0;j<col;j+=1)
       {
          if(arr[i][j]!=arr[i+1][j+1])

            {
                printf("Not a Toeplitz Matrix");
                return 0;
            }
        }
    }
    printf("Toeplitz Matrix");
    return 0;
}

OUTPUT
3 3
1 2 3
4 5 6
7 8 9
Not a Toeplitz Matrix